<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.telesnascimento</groupId>
    <artifactId>smallrye-ft-owb-interceptor-binding</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>SmallRye Fault Tolerance + OpenWebBeans interceptor-binding reproducer</name>
    <description>
        Reproduces the interceptor problem behind smallrye/smallrye-fault-tolerance#711:
        OpenWebBeans 4.1.0 does not apply a transitive interceptor binding that a portable
        extension registers programmatically (Weld and OpenWebBeans 4.0.3 do). The standalone
        cdi-isolation module is the SmallRye-free reproducer.
    </description>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <version.smallrye-ft>6.9.0</version.smallrye-ft>
        <version.weld-se>5.1.3.Final</version.weld-se>
        <version.owb>4.1.0</version.owb>
        <version.junit>5.10.2</version.junit>
        <version.surefire>3.2.5</version.surefire>
    </properties>

    <dependencies>
        <!-- SmallRye Fault Tolerance: brings the CDI extension (FaultToleranceExtension),
             the interceptor, the MP FT API and SmallRye core transitively -->
        <dependency>
            <groupId>io.smallrye</groupId>
            <artifactId>smallrye-fault-tolerance</artifactId>
            <version>${version.smallrye-ft}</version>
        </dependency>

        <!-- MP FT API (explicit, for @Retry) -->
        <dependency>
            <groupId>org.eclipse.microprofile.fault-tolerance</groupId>
            <artifactId>microprofile-fault-tolerance-api</artifactId>
            <version>4.0.2</version>
        </dependency>

        <!-- MicroProfile Config implementation: SmallRye FT reads config at bootstrap
             (FaultToleranceExtension calls ConfigProvider.getConfig()), so a ConfigProviderResolver
             must be present even for this minimal reproducer. -->
        <dependency>
            <groupId>io.smallrye.config</groupId>
            <artifactId>smallrye-config</artifactId>
            <version>3.10.2</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${version.junit}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <!-- Control: Weld SE (the CDI RI). Expected to PASS. -->
        <profile>
            <id>weld</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.jboss.weld.se</groupId>
                    <artifactId>weld-se-core</artifactId>
                    <version>${version.weld-se}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>

        <!-- Under test: OpenWebBeans SE (the CDI impl used by Apache TomEE). -->
        <profile>
            <id>owb</id>
            <dependencies>
                <dependency>
                    <groupId>org.apache.openwebbeans</groupId>
                    <artifactId>openwebbeans-se</artifactId>
                    <version>${version.owb}</version>
                    <scope>test</scope>
                </dependency>
                <dependency>
                    <groupId>org.apache.openwebbeans</groupId>
                    <artifactId>openwebbeans-impl</artifactId>
                    <version>${version.owb}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${version.surefire}</version>
            </plugin>
        </plugins>
    </build>
</project>
